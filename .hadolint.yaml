ignored:
  # Pinning apt package versions guarantees the build breaks the next time
  # Debian issues a security update, which is worse than the drift it prevents.
  # The base image tag is pinned instead, and Dependabot bumps it.
  - DL3008
